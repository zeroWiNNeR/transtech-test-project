# transtech-test-project

### Technology stack:
* Spring Boot
* Spring Web
* Spring Data
* PostgreSQL database

#### To build this application use:
```bash
mvn clean package [-DskipTests]
```

#### To run this application with docker-compose use:
```bash
docker-compose up
```
